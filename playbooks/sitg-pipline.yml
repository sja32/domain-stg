---
- name: Run Evaluate-STIG scan and upload results
  hosts: all
  gather_facts: no
  vars:
    share_path: "\\\\192.168.10.198\\STIG-Results"
    smb_user: "VoughtNet\\svc-ansible"
    smb_password: "Luasi965!"

  tasks:

    - name: Ensure Evaluate-STIG.ps1 exists
      ansible.windows.win_stat:
        path: 'C:\ProgramData\Evaluate-STIG\Evaluate-STIG.ps1'
      register: stig_script_check

    - name: ❌ Fail if Evaluate-STIG.ps1 is missing
      ansible.builtin.fail:
        msg: "Evaluate-STIG.ps1 is missing on {{ inventory_hostname }}. Run prep-stig-host first."
      when: not stig_script_check.stat.exists

    - name: ▶️ Run Evaluate-STIG scan
      ansible.windows.win_shell: |
        C:\ProgramData\Evaluate-STIG\Evaluate-STIG.ps1 `
          -output ckl, csv
      args:
        chdir: 'C:\ProgramData\Evaluate-STIG'

    ###################################################################
    # Determine Group-Based Folder Target
    ###################################################################
    - name: Set role folder name based on group
      set_fact:
        role_folder: |
          {% if 'domain_controllers' in group_names %}
          Domain-Controllers
          {% elif 'member_servers' in group_names %}
          Member-Servers
          {% else %}
          Workstations
          {% endif %}

    ###################################################################
    # Create Share Path + Upload Results
    ###################################################################
        - name: Ensure SMB destination folder exists
      ansible.windows.win_shell: |
        net use R: "{{ smb_share_root }}"
        if (!(Test-Path "R:\\{{ group_names[0] }}")) { New-Item -Path "R:\\{{ group_names[0] }}" -ItemType Directory -Force | Out-Null }
        if (!(Test-Path "R:\\{{ group_names[0] }}\\{{ inventory_hostname }}")) { New-Item -Path "R:\\{{ group_names[0] }}\\{{ inventory_hostname }}" -ItemType Directory -Force | Out-Null }
        net use R: /delete /y
      args:
        executable: PowerShell.exe

    - name: 📤 Copy STIG results to SMB share
      ansible.windows.win_shell: |
        net use R: "{{ smb_share_root }}"
        Copy-Item "{{ stig_results_local }}\\{{ inventory_hostname }}\\Checklist\\*" "R:\\{{ group_names[0] }}\\{{ inventory_hostname }}" -Recurse -Force
        net use R: /delete /y
      args:
        executable: PowerShell.exe


    - name: ✅ Summary
      ansible.builtin.debug:
        msg:
          - "✅ STIG Scan completed on {{ inventory_hostname }}."
          - "📤 Uploaded to: {{ share_path }}\\{{ role_folder }}\\{{ inventory_hostname }}"
